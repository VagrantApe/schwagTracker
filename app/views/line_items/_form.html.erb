<%= form_for ([@order, @line_item], remote: true) do |f| %>
  <%= f.collection_select :product_id, Product.all, :id, :name, prompt: true %><%= f.input :qty %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
  </div>
<% end %>
